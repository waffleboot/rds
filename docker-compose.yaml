version: "3.9"
services:
  some-redis:
    container_name: some-redis
    image: redis
    command: redis-server /data/redis.conf
    volumes:
      - ".:/data"
networks:
  default:
    name: some-network
