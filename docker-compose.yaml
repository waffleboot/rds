version: "3.9"
services:
  redis:
    container_name: redis
    image: yangand/redis:latest
    command: redis-server /data/redis.conf
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - ".:/data"
